{
  "name": "macbook-wiki",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "marked": "^14.1.3",
    "marked-gfm-heading-id": "^4.1.4"
  }
}
